apiVersion: v1
kind: Service
metadata:
  name: cost-analyzer
  labels:
    app: cost-analyzer
spec:
  type: ClusterIP
  selector:
    app: cost-analyzer
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
